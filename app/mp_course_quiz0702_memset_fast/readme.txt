* 문제: 메모리 영역을 특정 값으로 채우는 함수를 구현하십시오.      
    + 4 비이트 단위로 메모리에 값을 쓰는 str 명령을 사용합니다. (빠른 수행을 위해) 
    + 4 바이트 단위로 정렬이 되어있지 않은 시작 부분과 끝부분은 1 바이트 단위로 메모리에 값을 쓰는 strb 명령을 사용합니다.

* 참고 사항
    + 자세한 함수 정의는 파일, memset_fast.h에 있습니다. 
    + 함수 구현은 파일, memset_fast.S에 하면 됩니다.
    + memset_fast.S에 포함된 코드와 주석은 구현 예입니다.
      이대로 구현해도 되고, 자신만의 방식으로 구현해도 됩니다. 
